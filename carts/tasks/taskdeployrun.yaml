apiVersion: tekton.dev/v1beta1
kind: TaskRun
metadata:
  name: carts-final-run
spec:
  serviceAccountName: cart-svc-acc
  taskRef:
    name: carts-deploy-kubectl
  resources:
    inputs:
      - name: carts-resource
        resourceSpec:
          type: git
          params:
            - name: url
              value: https://github.com/walaafahad1994/level3-project

