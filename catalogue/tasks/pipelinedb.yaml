apiVersion: tekton.dev/v1beta1
kind: Pipeline
metadata:
  name: catlogdb-pipeline
spec:
  resources:
    - name: user-resource
      type: git
  tasks:
    - name: build
      taskRef:
        name: catalogue-db
      resources:
        inputs: 
        - name: catalogue-resource
          resource: catalogue-resource
    - name: user-deploy-kubectl
      runAfter: ["build"]
      taskRef:
        name: user-db-deploy-kubectl
      resources:
        inputs:
        - name: catalogue-resource
          resource: catalogue-resource

