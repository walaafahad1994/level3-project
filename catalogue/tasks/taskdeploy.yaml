apiVersion: tekton.dev/v1beta1
kind: Task
metadata:
  name: catlog-deploy-kubectl
  serviceAccountName: front-svc-acc
spec:
  params:
    - name: pathToYamlFile
      type: string
      description: The path to the yaml file to deploy within the git source
      default: $(resources.inputs.catalogue-resource.path)/catalogue/tasks/catlog-svc.yaml
    - name: namespace
      default: test
  resources:
    inputs:
      - name: catalogue-resource
        type: git
  steps:
   - name: run-kubectl
     image: lachlanevenson/k8s-kubectl
     command: ["kubectl"]
     args:
       - "apply"
       - "-f"
       - "$(params.pathToYamlFile)"
       - "-n"
       - "$(params.namespace)"
