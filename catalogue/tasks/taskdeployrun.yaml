apiVersion: tekton.dev/v1beta1
kind: TaskRun
metadata:
  name: catlog-final-run
spec:
  serviceAccountName: front-svc-acc
  taskRef:
    name:  catalogue-db-deploy
  resources:
    inputs:
      - name: catalogue-resource
        resourceSpec:
          type: git
          params:
            - name: url
              value: https://github.com/walaafahad1994/level3-project
