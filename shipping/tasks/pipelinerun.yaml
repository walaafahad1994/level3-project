apiVersion: tekton.dev/v1alpha1
kind: PipelineRun
metadata:
  name: ship-run
spec:
  serviceAccountName: front-svc-acc
  pipelineRef:
    name: ship-pipeline
  resources:
    - name: ship-resource
      resourceRef:
        name: ship-resource
