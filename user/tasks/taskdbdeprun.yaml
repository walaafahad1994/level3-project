apiVersion: tekton.dev/v1beta1
kind: TaskRun
metadata:
  name: userdb-final-run
spec:
  serviceAccountName: front-svc-acc
  taskRef:
    name: user-db-deploy-kubectl
  resources:
    inputs:
      - name: user-resource
        resourceSpec:
          type: git
          params:
            - name: url
              value: https://github.com/walaafahad1994/level3-project

