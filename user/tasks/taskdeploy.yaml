apiVersion: tekton.dev/v1beta1
kind: Task
metadata:
  name: user-deploy-kubectl
  serviceAccountName: user-svc-acc
spec:
  params:
    - name: pathToYamlFile
      type: string
      description: The path to the yaml file to deploy within the git source
      default: $(resources.inputs.user-resource.path)/user/tasks/user-svc-deb.yaml
    - name: pathToDbYamlFile
      type: string
      default:  $(resources.inputs.user-resource.path)/user/tasks/userdb-svc.yaml
    - name: namespace
      default: test
  resources:
    inputs:
      - name: user-resource
        type: git
  steps:
   - name: run-kubectl
     image: lachlanevenson/k8s-kubectl
     command: ["kubectl"]
     args:
       - "apply"
       - "-f"
       - "$(params.pathToDbYamlFile)"
       - "-f"
       - "$(params.pathToYamlFile)"
       - "-n"
       - "$(params.namespace)"
