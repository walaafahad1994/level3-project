apiVersion: tekton.dev/v1beta1
kind: TaskRun
metadata:
  name: user-final-run
spec:
  serviceAccountName: user-svc-acc
  taskRef:
    name: users-deploy-kubectl
  resources:
    inputs:
      - name: user-resource
        resourceSpec:
          type: git
          params:
            - name: url
              value: https://github.com/walaafahad1994/level3-project

