apiVersion: tekton.dev/v1beta1
kind: TaskRun
metadata:
  name: user-taskrun
spec:
  serviceAccountName: user-svc-acc 
  taskRef:
    name: user
  params:
  - name: IMAGE
    value: "walaafahad/users"
  resources:
    inputs:
    - name: users-resource
      resourceRef:
        name: user-resource 
